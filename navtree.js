var NAVTREE =
[
  [ "G+Smo", "index.html", [
    [ "Modules", "group__Modules.html", null ],
    [ "Groups and hierarchies", "usergroup0.html", [
      [ "Tutorials", "Tutorials.html", "Tutorials" ],
      [ "Examples", "Examples.html", "Examples" ],
      [ "Geometries hierarchy", "group__geometry.html", null ],
      [ "Bases hierarchy", "group__basis.html", null ],
      [ "Functions hierarchy", "group__function.html", null ],
      [ "PDE definitions", "group__pdeclass.html", null ],
      [ "Combinatorics group", "group__combinatorics.html", null ],
      [ "Enumeration declarations", "group__enum.html", null ]
    ] ],
    [ "Class List", "annotated.html", "annotated" ],
    [ "Python bindings", "python.html", null ],
    [ "Bibliographic references", "citelist.html", null ],
    [ "All groups", "modules.html", "modules" ]
  ] ]
];

var NAVTREEINDEX =
[
".html",
"classgismo_1_1expr_1_1tvar1__expr.html#afeaa6456e370d2eb59f5e139d9f8b00ca8091f05f46291202b6137999cd93dd78",
"classgismo_1_1gsALMCrisfield.html#a935c0528553e63e164fc95e1364d4106",
"classgismo_1_1gsAbsError.html#aace142da137e23b4218993b511db2128",
"classgismo_1_1gsBSpline.html#a077509d06123c1b38e6096f6a1c64069",
"classgismo_1_1gsBSplineBasis.html#a097d4b3daba2e740fd091aaa6642b4b2",
"classgismo_1_1gsBSplineBasis.html#ad50edfc4d9131a1a64ca2fd70535c930",
"classgismo_1_1gsBasis.html#a9cc2e0ddd22571bb205478589a64101d",
"classgismo_1_1gsBiharmonicAssembler.html#a8b3082b16e2ba4a8568f4592b02a2334",
"classgismo_1_1gsBulk.html#a26e19c44c309f3e76a1a50c3aca0557c",
"classgismo_1_1gsCPPInterface.html#a9c67d06925b0013b54667ac8db264482",
"classgismo_1_1gsConstantBasis.html#a7d711ed20a33a57f02120fa53000aa40",
"classgismo_1_1gsConstantFunction.html#ab0699c7c87b3708d25383bd3c268ed68",
"classgismo_1_1gsCurve.html#a9d0e2f7b0331fee8eef9789f5115a72d",
"classgismo_1_1gsDetFunction.html#a7d711ed20a33a57f02120fa53000aa40",
"classgismo_1_1gsDynamicNewmark.html#a2c91d03b7031f76509dd25d8f0d52cda",
"classgismo_1_1gsElasticityAssembler.html#a97c42437ed8b462b3e103117c03b641b",
"classgismo_1_1gsFileData.html#adf413c975b861f877794ea65965c81ab",
"classgismo_1_1gsFunctionAdaptor.html#aa393507438ecd85137d08e56fc162809",
"classgismo_1_1gsGenericAssembler.html#ab16422a35f35383205fc67a569c3e1b2",
"classgismo_1_1gsGeometry.html#a67530e7f13b644b82ff379805a9bb140",
"classgismo_1_1gsGeometryTransform.html#a67530e7f13b644b82ff379805a9bb140",
"classgismo_1_1gsHBSpline.html#a63475373ad95dd9c62aabb5a386e2b8b",
"classgismo_1_1gsHBSplineBasis.html#a5e16527854133572e342dfe21d3133a6",
"classgismo_1_1gsHBox.html#abede83174e726efefd9c05c80fd87172",
"classgismo_1_1gsHTensorBasis.html#a1a1aafeb3a74b68f58d2a567ed839b7a",
"classgismo_1_1gsHTensorBasis.html#ae4f090c7641592de992ddbb52e1f4f6b",
"classgismo_1_1gsIncompleteLUOp.html#ad88ec40c10dde3d1088cbbb9484e8e5e",
"classgismo_1_1gsKnotVector.html#ab020849b089c7d290f89ddbccbdfc51a",
"classgismo_1_1gsLagrangePoly.html#ad70a6973b0a31831cf597fada7371255",
"classgismo_1_1gsMPBESBSplineBasis.html#a2d1f0c0b58f4d46727c6d2c179063d87",
"classgismo_1_1gsMPBESHSplineBasis.html#ad20056b9e1bee1761e86542d38ca079d",
"classgismo_1_1gsMaterialMatrixBase.html#a21438d99ee154d18e93592a3ba7f36ff",
"classgismo_1_1gsMaterialMatrixComposite.html#adb68f4db05cd93138869b61ac07fee10",
"classgismo_1_1gsMaterialMatrixLinear.html#a9c74ea526ea94fd699a7f463c678b4c3",
"classgismo_1_1gsMaterialMatrixNonlinear.html#aeef9ad93c8a6c086315068a92de4d984",
"classgismo_1_1gsMinimalResidual.html#afc3e86efa72d15e2385ff898a6de363b",
"classgismo_1_1gsMultiGridOp.html#adb69a688050a3a1093664ed8b027b893",
"classgismo_1_1gsMvLegendreBasis.html#a57840c996d92c9162995c5fbd8ab1bd1",
"classgismo_1_1gsNsTimeIntegrator.html#a97419f5b9ef24271b6ca7667f3586ea0",
"classgismo_1_1gsNurbs.html#afda8b8e692431b8010a84ccaa11c53e1",
"classgismo_1_1gsNurbsBasis.html#ae829022bde8b0eeb995ae63d0a80ca64",
"classgismo_1_1gsOverlapCompare.html",
"classgismo_1_1gsPeriodicStitch.html#a803f3e75745bb6771ed8205c2a395fc8",
"classgismo_1_1gsPreconditionerOp.html#a27fb1b22169ec08f0ac8766443ecafba",
"classgismo_1_1gsRationalBasis.html#ace7e41a211c6f0ce892e6fd282f9ae96",
"classgismo_1_1gsSmoothInterfaces.html#a3bf55e9f819607c9bae1d902c105e223",
"classgismo_1_1gsStaticBase.html#ae80fb8338d210174448f7ab8595aec95",
"classgismo_1_1gsSurfMesh.html#a627feed9829449487411f4cf000c737d",
"classgismo_1_1gsSurface.html#a622bb1f6008bc8e3f31cd79651fddee2",
"classgismo_1_1gsTHBSpline.html#a9aa90c5d64dbe28bf4cf1ba1322e7a73",
"classgismo_1_1gsTHBSplineBasis.html#a63202bfcd72c7785e84c7be7b9df3061",
"classgismo_1_1gsTemplate.html#aa9dee7d24a54d8cb496e0af8bc642ede",
"classgismo_1_1gsTensorBSplineBasis.html#a0145c3cc9fc5a949474fca757a89aff0",
"classgismo_1_1gsTensorBSplineBasis.html#ad6c91dfd8a0efa08de25424b79782117",
"classgismo_1_1gsTensorBSplineBasis_3_011_00_01T_01_4.html#a9e75fdaf0af54d3432815d7befd0c106",
"classgismo_1_1gsTensorBasis.html#a9886df2e7a392a67dfe64dd3ff928f82",
"classgismo_1_1gsTensorNurbs.html#a6510573bfd428527ac2bfe1fa3d9ce1a",
"classgismo_1_1gsTensorNurbsBasis.html#a7d711ed20a33a57f02120fa53000aa40",
"classgismo_1_1gsThinShellAssembler.html#a728c6c903e78b707a47c7544d5625d6c",
"classgismo_1_1gsVisitorCDR.html#a0ea91504bea826a8b1cebf902841a094",
"classgismo_1_1gsVolume.html#aeab080a6c704b73349fbe152d1a70b02",
"classgismo_1_1trilinos_1_1solver_1_1AbstractDirectSolver.html",
"ieti_example.html#ieti_exampleAnnotated",
"structgismo_1_1gsMPBESBasis_1_1distances.html#a683d1a377f220503d4be50bdf5d4fd5e",
"structgismo_1_1stress__type.html#a7aead736a07eaf25623ad7bfa1f0ee2dafb852aa1dd1d8da296c52a3e39315c48"
];

var SYNCONMSG = 'click to disable panel synchronisation';
var SYNCOFFMSG = 'click to enable panel synchronisation';
var SYNCONMSG = 'click to disable panel synchronisation';
var SYNCOFFMSG = 'click to enable panel synchronisation';
var navTreeSubIndices = new Array();

function getData(varName)
{
  var i = varName.lastIndexOf('/');
  var n = i>=0 ? varName.substring(i+1) : varName;
  return eval(n.replace(/\-/g,'_'));
}

function stripPath(uri)
{
  return uri.substring(uri.lastIndexOf('/')+1);
}

function stripPath2(uri)
{
  var i = uri.lastIndexOf('/');
  var s = uri.substring(i+1);
  var m = uri.substring(0,i+1).match(/\/d\w\/d\w\w\/$/);
  return m ? uri.substring(i-6) : s;
}

function localStorageSupported()
{
  try {
    return 'localStorage' in window && window['localStorage'] !== null && window.localStorage.getItem;
  }
  catch(e) {
    return false;
  }
}


function storeLink(link)
{
  if (!$("#nav-sync").hasClass('sync') && localStorageSupported()) {
      window.localStorage.setItem('navpath',link);
  }
}

function deleteLink()
{
  if (localStorageSupported()) {
    window.localStorage.setItem('navpath','');
  } 
}

function cachedLink()
{
  if (localStorageSupported()) {
    return window.localStorage.getItem('navpath');
  } else {
    return '';
  }
}

function getScript(scriptName,func,show)
{
  var head = document.getElementsByTagName("head")[0]; 
  var script = document.createElement('script');
  script.id = scriptName;
  script.type = 'text/javascript';
  script.onload = func; 
  script.src = scriptName+'.js'; 
  if ($.browser.msie && $.browser.version<=8) { 
    // script.onload does not work with older versions of IE
    script.onreadystatechange = function() {
      if (script.readyState=='complete' || script.readyState=='loaded') { 
        func(); if (show) showRoot(); 
      }
    }
  }
  head.appendChild(script); 
}

function createIndent(o,domNode,node,level)
{
  var level=-1;
  var n = node;
  while (n.parentNode) { level++; n=n.parentNode; }
  if (node.childrenData) {
    var imgNode = document.createElement("img");
    imgNode.style.paddingLeft=(16*level).toString()+'px';
    imgNode.width  = 16;
    imgNode.height = 22;
    imgNode.border = 0;
    node.plus_img = imgNode;
    node.expandToggle = document.createElement("a");
    node.expandToggle.href = "javascript:void(0)";
    node.expandToggle.onclick = function() {
      if (node.expanded) {
        $(node.getChildrenUL()).slideUp("fast");
        node.plus_img.src = node.relpath+"ftv2pnode.png";
        node.expanded = false;
      } else {
        expandNode(o, node, false, false);
      }
    }
    node.expandToggle.appendChild(imgNode);
    domNode.appendChild(node.expandToggle);
    imgNode.src = node.relpath+"ftv2pnode.png";
  } else {
    var span = document.createElement("span");
    span.style.display = 'inline-block';
    span.style.width   = 16*(level+1)+'px';
    span.style.height  = '22px';
    span.innerHTML = '&nbsp;';
    domNode.appendChild(span);
  } 
}

var animationInProgress = false;

function gotoAnchor(anchor,aname,updateLocation)
{
  var pos, docContent = $('#doc-content');
  if (anchor.parent().attr('class')=='memItemLeft' ||
      anchor.parent().attr('class')=='fieldtype' ||
      anchor.parent().is(':header')) 
  {
    pos = anchor.parent().position().top;
  } else if (anchor.position()) {
    pos = anchor.position().top;
  }
  if (pos) {
    var dist = Math.abs(Math.min(
               pos-docContent.offset().top,
               docContent[0].scrollHeight-
               docContent.height()-docContent.scrollTop()));
    animationInProgress=true;
    docContent.animate({
      scrollTop: pos + docContent.scrollTop() - docContent.offset().top
    },Math.max(50,Math.min(500,dist)),function(){
      if (updateLocation) window.location.href=aname;
      animationInProgress=false;
    });
  }
}

function newNode(o, po, text, link, childrenData, lastNode)
{
  var node = new Object();
  node.children = Array();
  node.childrenData = childrenData;
  node.depth = po.depth + 1;
  node.relpath = po.relpath;
  node.isLast = lastNode;

  node.li = document.createElement("li");
  po.getChildrenUL().appendChild(node.li);
  node.parentNode = po;

  node.itemDiv = document.createElement("div");
  node.itemDiv.className = "item";

  node.labelSpan = document.createElement("span");
  node.labelSpan.className = "label";

  createIndent(o,node.itemDiv,node,0);
  node.itemDiv.appendChild(node.labelSpan);
  node.li.appendChild(node.itemDiv);

  var a = document.createElement("a");
  node.labelSpan.appendChild(a);
  node.label = document.createTextNode(text);
  node.expanded = false;
  a.appendChild(node.label);
  if (link) {
    var url;
    if (link.substring(0,1)=='^') {
      url = link.substring(1);
      link = url;
    } else {
      url = node.relpath+link;
    }
    a.className = stripPath(link.replace('#',':'));
    if (link.indexOf('#')!=-1) {
      var aname = '#'+link.split('#')[1];
      var srcPage = stripPath($(location).attr('pathname'));
      var targetPage = stripPath(link.split('#')[0]);
      a.href = srcPage!=targetPage ? url : "javascript:void(0)"; 
      a.onclick = function(){
        storeLink(link);
        if (!$(a).parent().parent().hasClass('selected'))
        {
          $('.item').removeClass('selected');
          $('.item').removeAttr('id');
          $(a).parent().parent().addClass('selected');
          $(a).parent().parent().attr('id','selected');
        }
        var anchor = $(aname);
        gotoAnchor(anchor,aname,true);
      };
    } else {
      a.href = url;
      a.onclick = function() { storeLink(link); }
    }
  } else {
    if (childrenData != null) 
    {
      a.className = "nolink";
      a.href = "javascript:void(0)";
      a.onclick = node.expandToggle.onclick;
    }
  }

  node.childrenUL = null;
  node.getChildrenUL = function() {
    if (!node.childrenUL) {
      node.childrenUL = document.createElement("ul");
      node.childrenUL.className = "children_ul";
      node.childrenUL.style.display = "none";
      node.li.appendChild(node.childrenUL);
    }
    return node.childrenUL;
  };

  return node;
}

function showRoot()
{
  var headerHeight = $("#top").height();
  var footerHeight = $("#nav-path").height();
  var windowHeight = $(window).height() - headerHeight - footerHeight;
  (function (){ // retry until we can scroll to the selected item
    try {
      var navtree=$('#nav-tree');
      navtree.scrollTo('#selected',0,{offset:-windowHeight/2});
    } catch (err) {
      setTimeout(arguments.callee, 0);
    }
  })();
}

function expandNode(o, node, imm, showRoot)
{
  if (node.childrenData && !node.expanded) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        expandNode(o, node, imm, showRoot);
      }, showRoot);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      } if (imm || ($.browser.msie && $.browser.version>8)) { 
        // somehow slideDown jumps to the start of tree for IE9 :-(
        $(node.getChildrenUL()).show();
      } else {
        $(node.getChildrenUL()).slideDown("fast");
      }
      if (node.isLast) {
        node.plus_img.src = node.relpath+"ftv2mlastnode.png";
      } else {
        node.plus_img.src = node.relpath+"ftv2mnode.png";
      }
      node.expanded = true;
    }
  }
}

function glowEffect(n,duration)
{
  n.addClass('glow').delay(duration).queue(function(next){
    $(this).removeClass('glow');next();
  });
}

function highlightAnchor()
{
  var aname = $(location).attr('hash');
  var anchor = $(aname);
  if (anchor.parent().attr('class')=='memItemLeft'){
    var rows = $('.memberdecls tr[class$="'+
               window.location.hash.substring(1)+'"]');
    glowEffect(rows.children(),300); // member without details
  } else if (anchor.parents().slice(2).prop('tagName')=='TR') {
    glowEffect(anchor.parents('div.memitem'),1000); // enum value
  } else if (anchor.parent().attr('class')=='fieldtype'){
    glowEffect(anchor.parent().parent(),1000); // struct field
  } else if (anchor.parent().is(":header")) {
    glowEffect(anchor.parent(),1000); // section header
  } else {
    glowEffect(anchor.next(),1000); // normal member
  }
  gotoAnchor(anchor,aname,false);
}

function selectAndHighlight(hash,n)
{
  var a;
  if (hash) {
    var link=stripPath($(location).attr('pathname'))+':'+hash.substring(1);
    a=$('.item a[class$="'+link+'"]');
  }
  if (a && a.length) {
    a.parent().parent().addClass('selected');
    a.parent().parent().attr('id','selected');
    highlightAnchor();
  } else if (n) {
    $(n.itemDiv).addClass('selected');
    $(n.itemDiv).attr('id','selected');
  }
  if ($('#nav-tree-contents .item:first').hasClass('selected')) {
    $('#nav-sync').css('top','30px');
  } else {
    $('#nav-sync').css('top','5px');
  }
  showRoot();
}

function showNode(o, node, index, hash)
{
  if (node && node.childrenData) {
    if (typeof(node.childrenData)==='string') {
      var varName    = node.childrenData;
      getScript(node.relpath+varName,function(){
        node.childrenData = getData(varName);
        showNode(o,node,index,hash);
      },true);
    } else {
      if (!node.childrenVisited) {
        getNode(o, node);
      }
      $(node.getChildrenUL()).css({'display':'block'});
      if (node.isLast) {
        node.plus_img.src = node.relpath+"ftv2mlastnode.png";
      } else {
        node.plus_img.src = node.relpath+"ftv2mnode.png";
      }
      node.expanded = true;
      var n = node.children[o.breadcrumbs[index]];
      if (index+1<o.breadcrumbs.length) {
        showNode(o,n,index+1,hash);
      } else {
        if (typeof(n.childrenData)==='string') {
          var varName = n.childrenData;
          getScript(n.relpath+varName,function(){
            n.childrenData = getData(varName);
            node.expanded=false;
            showNode(o,node,index,hash); // retry with child node expanded
          },true);
        } else {
          var rootBase = stripPath(o.toroot.replace(/\..+$/, ''));
          if (rootBase=="index" || rootBase=="pages" || rootBase=="search") {
            expandNode(o, n, true, true);
          }
          selectAndHighlight(hash,n);
        }
      }
    }
  } else {
    selectAndHighlight(hash);
  }
}

function removeToInsertLater(element) {
  var parentNode = element.parentNode;
  var nextSibling = element.nextSibling;
  parentNode.removeChild(element);
  return function() {
    if (nextSibling) {
      parentNode.insertBefore(element, nextSibling);
    } else {
      parentNode.appendChild(element);
    }
  };
}

function getNode(o, po)
{
  var insertFunction = removeToInsertLater(po.li);
  po.childrenVisited = true;
  var l = po.childrenData.length-1;
  for (var i in po.childrenData) {
    var nodeData = po.childrenData[i];
    po.children[i] = newNode(o, po, nodeData[0], nodeData[1], nodeData[2],
      i==l);
  }
  insertFunction();
}

function gotoNode(o,subIndex,root,hash,relpath)
{
  var nti = navTreeSubIndices[subIndex][root+hash];
  o.breadcrumbs = $.extend(true, [], nti ? nti : navTreeSubIndices[subIndex][root]);
  if (!o.breadcrumbs && root!=NAVTREE[0][1]) { // fallback: show index
    navTo(o,NAVTREE[0][1],"",relpath);
    $('.item').removeClass('selected');
    $('.item').removeAttr('id');
  }
  if (o.breadcrumbs) {
    o.breadcrumbs.unshift(0); // add 0 for root node
    showNode(o, o.node, 0, hash);
  }
}

function navTo(o,root,hash,relpath)
{
  var link = cachedLink();
  if (link) {
    var parts = link.split('#');
    root = parts[0];
    if (parts.length>1) hash = '#'+parts[1];
    else hash='';
  }
  if (hash.match(/^#l\d+$/)) {
    var anchor=$('a[name='+hash.substring(1)+']');
    glowEffect(anchor.parent(),1000); // line number
    hash=''; // strip line number anchors
    //root=root.replace(/_source\./,'.'); // source link to doc link
  }
  var url=root+hash;
  var i=-1;
  while (NAVTREEINDEX[i+1]<=url) i++;
  if (i==-1) { i=0; root=NAVTREE[0][1]; } // fallback: show index
  if (navTreeSubIndices[i]) {
    gotoNode(o,i,root,hash,relpath)
  } else {
    getScript(relpath+'navtreeindex'+i,function(){
      navTreeSubIndices[i] = eval('NAVTREEINDEX'+i);
      if (navTreeSubIndices[i]) {
        gotoNode(o,i,root,hash,relpath);
      }
    },true);
  }
}

function showSyncOff(n,relpath)
{
    n.html('<img src="'+relpath+'sync_off.png" title="'+SYNCOFFMSG+'"/>');
}

function showSyncOn(n,relpath)
{
    n.html('<img src="'+relpath+'sync_on.png" title="'+SYNCONMSG+'"/>');
}

function toggleSyncButton(relpath)
{
  var navSync = $('#nav-sync');
  if (navSync.hasClass('sync')) {
    navSync.removeClass('sync');
    showSyncOff(navSync,relpath);
    storeLink(stripPath2($(location).attr('pathname'))+$(location).attr('hash'));
  } else {
    navSync.addClass('sync');
    showSyncOn(navSync,relpath);
    deleteLink();
  }
}

function initNavTree(toroot,relpath)
{
  var o = new Object();
  o.toroot = toroot;
  o.node = new Object();
  o.node.li = document.getElementById("nav-tree-contents");
  o.node.childrenData = NAVTREE;
  o.node.children = new Array();
  o.node.childrenUL = document.createElement("ul");
  o.node.getChildrenUL = function() { return o.node.childrenUL; };
  o.node.li.appendChild(o.node.childrenUL);
  o.node.depth = 0;
  o.node.relpath = relpath;
  o.node.expanded = false;
  o.node.isLast = true;
  o.node.plus_img = document.createElement("img");
  o.node.plus_img.src = relpath+"ftv2pnode.png";
  o.node.plus_img.width = 16;
  o.node.plus_img.height = 22;

  if (localStorageSupported()) {
    var navSync = $('#nav-sync');
    if (cachedLink()) {
      showSyncOff(navSync,relpath);
      navSync.removeClass('sync');
    } else {
      showSyncOn(navSync,relpath);
    }
    navSync.click(function(){ toggleSyncButton(relpath); });
  }

  $(window).load(function(){
    navTo(o,toroot,window.location.hash,relpath);
    showRoot();
  });

  $(window).bind('hashchange', function(){
     if (window.location.hash && window.location.hash.length>1){
       var a;
       if ($(location).attr('hash')){
         var clslink=stripPath($(location).attr('pathname'))+':'+
                               $(location).attr('hash').substring(1);
         a=$('.item a[class$="'+clslink+'"]');
       }
       if (a==null || !$(a).parent().parent().hasClass('selected')){
         $('.item').removeClass('selected');
         $('.item').removeAttr('id');
       }
       var link=stripPath2($(location).attr('pathname'));
       navTo(o,link,$(location).attr('hash'),relpath);
     } else if (!animationInProgress) {
       $('#doc-content').scrollTop(0);
       $('.item').removeClass('selected');
       $('.item').removeAttr('id');
       navTo(o,toroot,window.location.hash,relpath);
     }
  })
}

